#include <bits/stdc++.h>
using namespace std;

int n, c, arr[1001];
map<int, vector<pair<int, int>>> mp;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    cin >> n >> c;

    for(int i = 0; i < n; i++) {
        cin >> arr[i];
    }


    return 0;
}